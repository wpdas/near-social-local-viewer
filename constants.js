const defaultCodeMessage = `
return (
  <div>
    <p style={{ fontWeight: 600, color: "#AB2E28", fontFamily: "Courier new" }}>
      No Widget file found!
    </p>
  </div>
);
`;

module.exports = {
  defaultCodeMessage,
};
